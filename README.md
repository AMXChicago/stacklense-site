# StackLense

Dashboard spec lives at [`docs/blueprint-spec.md`](docs/blueprint-spec.md). Read it before working on anything in `features/canvas`, `features/inspector`, `features/activity`, or anywhere else in the dashboard layer.
