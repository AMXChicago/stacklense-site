/**
 * GitHub connect placeholder.
 *
 * Real form + OAuth flow + repo picker live in features/connect/
 * once that flow is rebuilt.
 */
export default function ConnectGithubPage() {
  return (
    <div className="mx-auto max-w-2xl px-6 py-12">
      <p className="font-mono text-xs text-ink3">
        /dashboard/connect/github placeholder
      </p>
      <h1 className="mt-2 text-2xl font-semibold">Connect via GitHub</h1>
      <p className="mt-2 text-sm text-ink2">
        The connect form is being rebuilt in features/connect/.
      </p>
    </div>
  );
}
